'use client';

/**
 * Default Next.js fallback loading UI.
 * Returning null allows the persistent Layout (with Header progress bar)
 * to handle the visual indication.
 */
export default function Loading() {
  return null;
}
